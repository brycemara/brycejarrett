import Header from "@/components/Header";
import Landing from "@/components/Landing";

export const Home = () => {
  return (
    <div>
      <Header />
      <Landing />
    </div>
  );
};

export default Home;
